# ---------------------------------------------------------------------------
# paper2code 依赖
#
# 设计原则：**核心流程全部可用标准库运行**（离线模式 + 内置 SVG 图表 + 无音频），
# 下面的包都是可选增强，缺失时对应能力自动降级并在产物里写明原因。
# ---------------------------------------------------------------------------

# --- 配置 ---
pyyaml>=6.0                # configs/default.yaml（缺失仍可用内置默认值）

# --- 论文解析（缺失时 PDF 解析降级为空结构，Markdown/HTML 链路不受影响） ---
pymupdf>=1.24              # PDF 正文与图像
pdfplumber>=0.11           # PDF 表格（对网格线表格更稳）

# --- 复现与可视化 ---
matplotlib>=3.8            # 图表 PNG 渲染（缺失时自动使用内置纯 SVG 渲染器）
numpy>=1.24                # 仅部分示意脚本使用

# --- 讲解：知识播客语音（免费、免 API Key） ---
edge-tts>=6.1              # 缺失时仅产出脚本与 SRT 字幕

# --- 本地 Web UI ---
fastapi>=0.110
uvicorn>=0.29

# --- 网络抓取（arXiv；缺失时回退 urllib） ---
requests>=2.31
python-multipart>=0.0.9
