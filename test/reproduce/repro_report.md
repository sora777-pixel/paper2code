# 复现报告：Where Does the Cache Hit Ratio Go?

- 论文 ID：`ui_click_test`
- 情形判定：**情形 B · 无任何代码**（置信度 0.7）
- 判定依据：无代码、无伪代码，只能依据正文与表格数据重建
  - 未发现代码链接，也未识别出伪代码算法块
  - 可用输入：论文中的 2 张表格 + 附带数据文件

## 一、复现策略

论文既无代码也无伪代码，可复现的上限是**数据自洽性与可视化复刻**。我们以论文表格与附带数据为唯一事实来源，重建 tidy 数据、校验其内部一致性（占比求和、汇总行、最优性声明），并重绘图表。数据集、超参、预处理等未披露要素会被列成清单，供人工补齐后二次复现。

【模型补充】优先从 T1 反推内容类别与目标统计量，用合成或重采样生成 <1 万条请求记录：按请求份额分配各类别请求数，再依请求命中率随机标记命中，并为每类赋予合理字节大小以匹配字节份额。随后按类别汇总，计算请求/字节份额与命中率，与 T1 对比校正；用同一汇总绘制 F1，即每类请求份额与字节份额的对比（双柱或散点）。T2 的总请求量只能记录原始值，单机小数据下无法复现。主要风险：无原始数据与代码，类别边界和字节分布未知，命中率随机标记引入偏差，小样本比例不稳定，F1 精确形态可能失真。应固定随机种子、报告多次抽样波动，并明确标注缩放与不可复现项。

### 执行步骤

1. 抽取论文中的全部表格与附带数据文件，整理为 tidy CSV
2. 对论文数据做内部一致性校验（占比求和、汇总行、最优性声明、排名合法性）
3. 以正文描述为线索重绘论文中的图表，做风格复刻
4. 输出「无法复现要素清单」：缺失的数据集、超参、随机种子、预处理细节

## 二、复现目标清单

| 类型 | ID | 说明 | 规模 | 角色 |
| --- | --- | --- | --- | --- |
| figure | F1 | Request share versus byte share by content class. The two | - | 讲解引用 |

## 三、数据输入

_未抽取到表格数据。_

## 四、无法复现要素清单

- 数据集/数据来源
- 超参数与训练配置
- 随机种子/重复次数
- 预处理与分词细节
- 硬件与运行环境

## 五、校验与对标明细

结论：**not_verifiable** ｜ pass 0 / warn 0 / fail 0 / skipped 0 ｜ 得分 0.0

_无可执行的校验项。_

## 六、图表重绘

_无可用数据重绘图表。_

## 七、规模约束与扩展性

- 本次复现的数据规模上限：10000 条记录
- 单次执行超时：120s
- 架构预留：执行器（reproduce.runner）与计划（reproduce.planner）解耦，后续可替换为容器/集群后端而无需改动用例定义

## 八、注意事项

- 以下要素在论文中未明确披露，无法复现：数据集/数据来源、超参数与训练配置、随机种子/重复次数、预处理与分词细节、硬件与运行环境。已列入报告，供人工补齐后二次复现。

---

_本报告由 paper2code 自动生成。数值对标使用相对误差阈值 pass ≤ 5%、warn ≤ 20%。_